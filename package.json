{
  "name": "parabank-automation-framework",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "playwright test -c config",
    "test:ui": "playwright test --ui -c config",
    "test:debug": "playwright test --debug -c config",
    "report:allure": "allure generate allure-results --clean -o allure-report && allure open allure-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^24.10.0",
    "playwright": "^1.56.1",
    "dotenv": "^16.4.5",
    "allure-playwright": "^2.15.1",
    "allure-commandline": "^2.29.0"
  }
}
