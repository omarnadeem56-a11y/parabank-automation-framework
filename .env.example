TEST_ENV=local
BASE_URL=http://localhost:8080/parabank
HEADLESS=false
TRACE=on
SLOW_MO=200
VIDEO=on-first-retry
SCREENSHOT=only-on-failure

# Default credentials (used by account overview tests)
DEFAULT_USER=
DEFAULT_PASS=
